name: DigitalTwin Lap2010_5_Pumpensteuerung

on:
  push:
    branches: [ main ]

env:
      PROJECT_PATH: PlcDigitalTwinAutoTest/DtLap2010_5_Pumpensteuerung/DtLap2010_5_Pumpensteuerung.csproj
      
jobs:
  deploy:
    runs-on: windows-latest 

    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.x

    - run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -o PublishMin

    - uses: actions/upload-artifact@v2
      with:
        name: DtLap2010_5_Pumpensteuerung Min
        path: PublishMin
